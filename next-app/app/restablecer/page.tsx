import { RequestResetForm } from '@/components/company-reset-forms';

export const metadata = { title: 'Restablecer contraseña — Uniform Logistic' };

export default function RequestResetPage() {
    return <RequestResetForm />;
}
